# Copy to .env and fill in. .env is gitignored -- never commit real credentials.
TRIAGE_BASE_URL=https://api.deepseek.com/v1
TRIAGE_API_KEY=***
TRIAGE_MODEL=deepseek-flash

# Optional: TypeSafe (Jev) typed per-actor judgments -- enables /actors/assessment and the
# analyst's assess_actor_intent tool. Get a key at https://console.typesafe.ai/
TYPESAFE_API_KEY=
