# Agent orchestration demo —— LangChain tools + CrewAI crew
# Only langchain-core is imported (the @tool decorator); the langchain and langchain-openai
# umbrella packages are not used — crewai owns the model client.
langchain-core>=0.3
crewai>=0.80
pydantic>=2.7
python-dotenv>=1.0   # triage_crew loads .env explicitly; don't rely on it arriving transitively

# 服务层（把 crew 暴露成一个可被外部触发的 HTTP 端点）
fastapi>=0.115
uvicorn>=0.32

# 测试
pytest>=8.3
httpx>=0.27          # fastapi.testclient.TestClient needs it; tests/test_server.py drives the routes
