apiVersion: v1
kind: Service
metadata:
  name: audit-triage
  namespace: audit-triage
spec:
  type: ClusterIP          # internal only; no need to expose this externally for the demo
  selector:
    app: audit-triage
  ports:
    - name: http
      port: 8000
      targetPort: 8000
