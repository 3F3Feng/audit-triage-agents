apiVersion: v1
kind: ConfigMap
metadata:
  name: audit-triage-config
  namespace: audit-triage
# Non-secret config only. The API key lives in a Secret (see secret.example.yaml).
data:
  TRIAGE_BASE_URL: "https://api.deepseek.com/v1"
  TRIAGE_MODEL: "deepseek-flash"
