apiVersion: v1
kind: ServiceAccount
metadata:
  name: audit-triage-sa
  namespace: audit-triage
# This service never calls the Kubernetes API, so it should not even receive a token.
# Turning automount off means no API credential is mounted into the pod at all.
automountServiceAccountToken: false
